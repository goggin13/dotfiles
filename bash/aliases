
# Applications
alias clojure="java -cp /Users/mattgoggin/Library/Clojure/clojure.jar clojure.main"
alias js="java -cp /Users/goggin/Documents/CS/rhino/js.jar:/Users/goggin/Documents/CS/rhino/jline.jar jline.ConsoleRunner org.mozilla.javascript.tools.shell.Main -opt -1"
alias watch_bnter_r="cd $CS/rails_projects/bnter_rails/public/javascripts; coffee --watch -o coffee_js/ -c coffee/"
alias watch_bnter_t="cd $CS/rails_projects/bnter_rails/spec/; coffee --watch -c javascripts/"
alias watch_bnter="cd $BNTER/web/assets/js; coffee --watch -o coffee_js/ -c coffee/"

# Navigation
alias ..="cd .."
alias cs="cd $CS"
alias bnter="cd $BNTER"
alias fchat="cd $FCHAT"
alias dot="cd $DOTFILES"
alias m="mate ."
alias h="history"
alias o="open ."


# Alias management
alias reload="source ~/.bashrc"

# Processes
alias tu='top -o cpu' # cpu
alias tm='top -o vsize' # memory

# Git
alias g='git status'
alias gaa='git add -A'
alias wipeout="git reset --hard HEAD; git pull"

function gd () {
	git diff $1
}

function ga () {
	git add $1
}

function gco () {
	git checkout $1
}

function gpu () {
	git push $1
}

function gp () {
	git pull $1
}

function gacp () {
	ga $1
	gg $2
}

function take {
	mkdir $1
	cd $1
}

